package org.icepdf.ri.common;

/**
 * This class creates all the swing components of DictionaryPane.
 * This class uses SwingDictionaryController to control the 
 * swing components behaviour.
 *
 **/

public class SwingDictionaryBuilder{
/*
	public void buildDictionaryFrame(){
		JFrame dictionaryFrame = new JFrame(new BorderLayout());
		dictionaryFrame.setDefaultCloseOperation(WindowConstants.EXIT_ON_CLOSE);

		JPanel bottomPanel = buildBottomPanel();
		if(bottomPanel != null)
			dictionaryFrame.add(bottomPanel, BorderLayout.SOUTH);
		
		JEditorPane resultEditorPane = new JEditorPane();
		dictionaryFrame.add(resultEditorPane, BorderLayout.CENTER);

		return dictionaryFrame;
	}
*/	
}